/* pages/2-11/2-11.wxss */

.child-box {
  display: flex;
  margin: 4px 8px;
}

.subtitle {
  margin-right: 4px;
  font-size: 16px;
  color: rgba(0, 0, 0, 0.6);
}

.unit {
  position: absolute;
  right: 24px;
}

input {
  border-bottom: 1px solid rgba(0, 0, 0, 0.6);
}