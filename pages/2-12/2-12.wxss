/* pages/2-12/2-12.wxss */

.child-box {
  margin: 4px 8px;
}

#sum-box {
  display: flex;
}

.subtitle {
  margin-right: 4px;
  font-size: 16px;
  color: rgba(0, 0, 0, 0.6);
}

.unit {
  position: absolute;
  right: 24px;
}

input {
  border-bottom: 1px solid rgba(0, 0, 0, 0.6);
}